node_modules/
dist/
.env
.env.PRODUCCION
*.log
.DS_Store
*.enc
backups/
.pm2/
.postgres_password.txt
logs/
.claude/

# Datos subidos por usuarios (facturas, fotos, SDS, etc.)
backend/uploads/
uploads/
backend/facturas-test/

# Paquetes para distribución a clientes
LogaERP-paquete-cliente.zip

# Tarifas originales (PDFs/Excel de las agencias). Los datos ya están en BD.
tarifasagenciatransportes/

# Ya no se versiona .env (logaerp.bat / install.ps1 lo crean automaticamente
# en Windows con puerto 5433; en Mac dev usar puerto 5432 manualmente)
